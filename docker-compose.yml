version: '3.8'
services:
  mysql-db:
    image: mysql:8.0.36
    container_name: esiee_taskmanager_db
    environment:
      MYSQL_DATABASE: taskmanager_db
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_USER: task_user
      MYSQL_PASSWORD: task_password
    ports:
      - "3306:3306"